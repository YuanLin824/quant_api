# API 文档

量化交易系统 API 接口文档。

## 目录

- [基础信息](#基础信息)
- [系统配置](./docs/api-config.md)
- [健康检查](./docs/api-health.md)
- [认证接口](./docs/api-auth.md)
- [通达信（TDX）接口](./docs/api-tdx.md)
- [证券数据（WeStock）](./docs/api-westock.md)

## 基础信息

- **基础路径**: `/api`（由 `API_PREFIX` 环境变量控制）
- **默认端口**: `3000`（`.env` 中由 `PORT` 覆盖，`.env.example` 的取值为 `3001`）
- **响应格式**: JSON
- **请求体大小限制**: 10KB

## 认证与限流

| 接口分组   | 路径前缀       | 认证要求                                       | 限流                 |
| ---------- | -------------- | ---------------------------------------------- | -------------------- |
| 健康检查   | `/api/health`  | 否                                             | 全局 60 秒 60 次     |
| 认证接口   | `/api/auth`    | 各接口不同，详见[认证接口](./docs/api-auth.md) | 各接口不同，见该文档 |
| 通达信 TDX | `/api/tdx`     | 是（access token）                             | 不限流               |
| 证券数据   | `/api/westock` | 是（access token）                             | 全局 60 秒 60 次     |

受保护接口统一通过请求头携带令牌（唯一例外是 `POST /api/auth/refresh`，它携带的是 refresh token）：

```
Authorization: Bearer <access_token>
```

> 行情类接口因数据为公开信息，已显式豁免限流。

## 通用响应格式

所有接口返回统一的响应格式：成功时 `data` 为业务数据，**失败时 `data` 恒为 `null`**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": { ... }
}
```

### 错误响应

`code` 与 HTTP 状态码保持一致，`message` 为具体的错误原因。

> 错误响应由全局异常过滤器直接拼装，键顺序为 `code` / `data` / `message`，
> 与成功响应（控制器返回值）的 `code` / `message` / `data` 不同。

```json
{
  "code": 401,
  "data": null,
  "message": "访问令牌无效或已过期"
}
```

### 参数校验失败

DTO 校验失败时，`message` 直接给出具体的校验错误；多条错误以 `; ` 连接：

```json
{
  "code": 400,
  "data": null,
  "message": "股票代码必须是数组"
}
```

## 错误码说明

| HTTP 状态码 | 说明                                                               |
| ----------- | ------------------------------------------------------------------ |
| 200         | 请求成功                                                           |
| 201         | 创建成功（注册）                                                   |
| 400         | 请求参数错误                                                       |
| 401         | 未授权或 token 已过期                                              |
| 403         | 禁止访问（账号被禁用、登录失败次数过多）                           |
| 404         | 资源不存在                                                         |
| 409         | 冲突（用户名已存在等唯一约束冲突）                                 |
| 429         | 请求过于频繁（触发限流，`message` 为「请求过于频繁，请稍后重试」） |
| 500         | 服务器内部错误                                                     |
| 503         | 依赖组件不可用（如行情服务器或 WeStock CLI 不可达）                |
| 504         | 上游超时（调用外部组件超时未返回）                                 |

## 数据库错误映射

底层 PostgreSQL 错误由全局异常过滤器转换为对应状态码与中文提示，不会向前端暴露数据库细节：

| 错误类型               | 状态码 | 提示消息                     |
| ---------------------- | ------ | ---------------------------- |
| 唯一约束冲突 (`23505`) | 409    | 数据已存在, 违反唯一约束     |
| 外键约束 (`23503`)     | 400    | 数据关联异常, 违反外键约束   |
| 非空约束 (`23502`)     | 400    | 必填字段缺失, 违反非空约束   |
| 检查约束 (`23514`)     | 400    | 数据校验失败, 违反检查约束   |
| 排他约束 (`23506`)     | 409    | 违反排他约束                 |
| 字段超长 (`22001`)     | 400    | 字段值超出最大长度限制       |
| 类型不匹配 (`22P02`)   | 400    | 字段类型不匹配, 数据格式无效 |
| 死锁 (`40P01`)         | 409    | 检测到死锁, 请重试           |
| 其他数据库异常         | 500    | 数据库操作异常               |
