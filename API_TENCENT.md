# 腾讯财经公开股市行情接口文档

> 整理时间：2026-09-24
> 说明：以下接口均为腾讯财经网页端/移动端常见的内部公开接口，并非腾讯官方正式文档化 API。腾讯可能随时调整或限制，建议做好异常处理和备用数据源方案。所有接口无需注册、无需 API Key，通过 HTTP GET 请求即可访问。
> 本文档仅保留已验证可用的接口。

---

## 目录

1. 基础实时行情接口
2. 盘口分析接口
3. 简要信息接口
4. 分时数据接口
5. K线数据接口
6. 板块/行业排名接口
7. 股票搜索接口
8. 指数与全球指数接口
9. 接口地址总览

---

## 一、基础实时行情接口

### 1.1 接口地址

```text
http://qt.gtimg.cn/q=股票代码
```

### 1.2 请求参数

| 参数 | 说明                             |
| ---- | -------------------------------- |
| `q`  | 股票代码，多个代码用英文逗号分隔 |

**代码前缀规范**：

| 市场     | 前缀 | 示例       |
| -------- | ---- | ---------- |
| 上海A股  | `sh` | `sh600519` |
| 深圳A股  | `sz` | `sz000001` |
| 港股     | `hk` | `hk00700`  |
| 美股     | `us` | `usBABA`   |
| 港股指数 | `hk` | `hkHSI`    |
| 上证指数 | `sh` | `sh000001` |
| 创业板指 | `sz` | `sz399006` |

**批量查询示例**：

```text
http://qt.gtimg.cn/q=sh600519,sz000001,hk00700
```

### 1.3 返回格式

纯文本，结构为：

```text
v_市场代码="字段1~字段2~字段3~...~字段N";
```

- 字段间以波浪号 `~` 分隔
- 编码为 **GBK**，需手动指定编码解码，否则中文名称乱码
- 字段下标从 **0** 开始

### 1.4 核心字段解析

以 A 股为例，返回约 49 个字段。关键字段索引及含义如下：

| 下标 | 字段含义           | 说明                           |
| ---- | ------------------ | ------------------------------ |
| 1    | 股票名称           | 需 GBK 解码                    |
| 2    | 股票代码           |                                |
| 3    | 当前价格           |                                |
| 4    | 昨收价             |                                |
| 5    | 今开价             |                                |
| 6    | 成交量（手）       |                                |
| 7    | 外盘               | 主动买入量                     |
| 8    | 内盘               | 主动卖出量                     |
| 9    | 买一价             |                                |
| 10   | 买一量（手）       |                                |
| 19   | 卖一价             |                                |
| 20   | 卖一量（手）       |                                |
| 29   | 最近逐笔成交       | 格式：`时间/价格/量/方向/金额` |
| 30   | 时间戳             | 格式：`YYYYMMDDHHMMSS`         |
| 31   | 涨跌额             |                                |
| 32   | 涨跌幅（%）        |                                |
| 33   | 今日最高价         |                                |
| 34   | 今日最低价         |                                |
| 35   | 价格/成交量/成交额 | 复合字段                       |
| 36   | 成交量（手）       |                                |
| 37   | 成交额（万元）     |                                |
| 38   | 换手率（%）        |                                |
| 39   | 市盈率（TTM）      |                                |
| 43   | 振幅（%）          |                                |
| 44   | 流通市值（亿）     |                                |
| 45   | 总市值（亿）       |                                |
| 46   | 市净率             |                                |
| 47   | 涨停价             |                                |
| 48   | 跌停价             |                                |

> 注意：港股和美股的部分字段会有所不同。港股通常以港元计价，美股以美元计价，展示时需标注正确货币单位。

---

## 二、盘口分析接口

### 2.1 接口地址

```text
http://qt.gtimg.cn/q=s_pk股票代码
```

**示例**：

```text
http://qt.gtimg.cn/q=s_pksh600519
```

### 2.2 返回格式

纯文本，结构为：

```text
v_s_pk市场代码="数值1~数值2~数值3~数值4";
```

### 2.3 返回参数说明

返回的是**盘口强弱比例数据**，共四个小数，分别代表买盘和卖盘在四个价格档位上的挂单量占比。

| 字段位置 | 含义             |
| -------- | ---------------- |
| 第 1 个  | 买一档挂单量占比 |
| 第 2 个  | 买二档挂单量占比 |
| 第 3 个  | 卖一档挂单量占比 |
| 第 4 个  | 卖二档挂单量占比 |

**示例**：`s_pksh600519` 返回 `0.307~0.171~0.379~0.143`，表示买一占比 30.7%，买二占比 17.1%，卖一占比 37.9%，卖二占比 14.3%。

### 2.4 支持范围

- **仅支持 A 股**（`sh` / `sz` 前缀）
- 港股、美股传入后返回 `v_pv_none_match="1";`
- 代码必须包含市场前缀，否则无法匹配

> 如需完整的买卖五档价格和挂单量，请使用基础行情接口 `http://qt.gtimg.cn/q=代码` 中的下标 9–28 字段。

---

## 三、简要信息接口

### 3.1 接口地址

```text
http://qt.gtimg.cn/q=s_股票代码
```

**示例**：

```text
http://qt.gtimg.cn/q=s_sz000001
```

### 3.2 返回格式

纯文本，结构类似基础行情接口，但字段更精简：

```text
v_s_市场代码="字段1~字段2~...~字段N";
```

### 3.3 返回参数说明

实测返回约 10 余个字段，常见字段如下，具体以实际返回为准：

| 下标 | 字段含义     | 说明               |
| ---- | ------------ | ------------------ |
| 1    | 股票名称     | 需 GBK 解码        |
| 2    | 股票代码     |                    |
| 3    | 当前价格     |                    |
| 4    | 涨跌额       |                    |
| 5    | 涨跌幅（%）  |                    |
| 6    | 成交量（手） |                    |
| 7    | 成交额       | 单位以实际返回为准 |

> 解析方式与基础行情一致：GBK 解码，`~` 分隔。

---

## 四、分时数据接口

### 4.1 接口地址

```text
https://web.ifzq.gtimg.cn/appstock/app/minute/query?code=股票代码
```

**示例**：

```text
https://web.ifzq.gtimg.cn/appstock/app/minute/query?code=sz159326
```

### 4.2 返回格式

标准 JSON 结构，包含以下一级字段：

| 字段   | 类型   | 说明                             |
| ------ | ------ | -------------------------------- |
| `code` | 数字   | 状态码，0=成功                   |
| `msg`  | 字符串 | 返回消息                         |
| `data` | 对象   | 核心行情数据，包含分时和基础信息 |
| `qt`   | 数组   | 盘口、价格、成交量等汇总明细     |

### 4.3 分时数组结构

`data.股票代码.data` 为逐分钟数据数组，每条字符串格式：

```text
时分 价格 累计成交量 累计成交额
```

**示例**：

```text
0930 2.023 37265 7538709.50
```

| 序号 | 字段       | 格式 | 说明               |
| ---- | ---------- | ---- | ------------------ |
| 1    | 交易时间   | HHMM | 如 0930 代表 9:30  |
| 2    | 分时价格   | 浮点 | 当前分钟最新成交价 |
| 3    | 累计成交量 | 整型 | 开盘至当前累计总量 |
| 4    | 累计成交额 | 浮点 | 开盘至当前累计总额 |

> 时间范围覆盖 09:30–15:00，1300 代表午后 13:00。成交量和成交额为累加值，非单分钟增量。

---

## 五、K线数据接口

### 5.1 日/周/月 K线（支持复权）

```text
https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=股票代码,周期,,,数量,复权类型
```

**参数说明**：

| 参数     | 说明                                           |
| -------- | ---------------------------------------------- |
| 股票代码 | 如 `sh600519`                                  |
| 周期     | `day` / `week` / `month`                       |
| 数量     | 返回K线根数                                    |
| 复权类型 | `qfq`（前复权）/ `hfq`（后复权）/ 空（不复权） |

**示例**：获取贵州茅台最近 10 根日K线（前复权）

```text
https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=sh600519,day,,,10,qfq
```

**返回参数说明**：

返回 JSON，核心数据位于 `data.股票代码.qfqday`（前复权）或 `data.股票代码.day`（不复权）等数组中。每个数组元素为一条 K 线，格式通常为：

```text
[日期, 开盘价, 收盘价, 最高价, 最低价, 成交量]
```

例如：

```text
["2026-09-23", 1255.03, 1251.24, 1271.50, 1250.89, 30981]
```

### 5.2 分钟 K线

```text
http://ifzq.gtimg.cn/appstock/app/kline/mkline?param=股票代码,周期,,,数量
```

**周期可选值**：

| 周期  | 说明    |
| ----- | ------- |
| `m1`  | 1 分钟  |
| `m5`  | 5 分钟  |
| `m15` | 15 分钟 |
| `m30` | 30 分钟 |
| `m60` | 60 分钟 |

**示例**：

```text
http://ifzq.gtimg.cn/appstock/app/kline/mkline?param=sh600519,m5,,,241
```

**返回参数说明**：

返回 JSON，核心数据位于 `data.股票代码.m5` 等数组中。每个数组元素通常为：

```text
[时间, 开盘价, 收盘价, 最高价, 最低价, 成交量]
```

---

## 六、板块/行业排名接口

### 6.1 接口地址

```text
https://proxy.finance.qq.com/cgi/cgi-bin/rank/pt/getRank?board_type={板块类型}&sort_type=priceChange&direct={方向}&offset=0&count={数量}
```

**参数说明**：

| 参数         | 说明                                                    |
| ------------ | ------------------------------------------------------- |
| `board_type` | 板块类型，如 `hy`（行业）、`gn`（概念）、`dy`（地域）等 |
| `direct`     | 排序方向，`up`=涨幅榜 / `down`=跌幅榜                   |
| `sort_type`  | 排序字段，如 `priceChange`                              |
| `offset`     | 偏移量，用于分页                                        |
| `count`      | 返回条数                                                |

**返回参数说明**：

返回 JSON，状态字段为 `code`、`msg`，核心数据位于 `data.rank_list` 数组。常见字段如下：

| 字段    | 含义             |
| ------- | ---------------- |
| `name`  | 板块名称         |
| `code`  | 板块代码         |
| `zdf`   | 涨跌幅（%）      |
| `zxj`   | 最新价           |
| `hsl`   | 换手率（%）      |
| `zsz`   | 总市值（亿）     |
| `ltsz`  | 流通市值（亿）   |
| `zljlr` | 主力净流入（元） |

---

## 七、股票搜索接口

### 7.1 接口地址

```text
https://smartbox.gtimg.cn/s3/?v=2&q={关键词}&t=all&c=1
```

### 7.2 参数说明

| 参数 | 说明                                         |
| ---- | -------------------------------------------- |
| `q`  | 搜索关键词，股票代码、名称拼音、中文名称均可 |
| `t`  | 搜索类型，`all` 表示全部                     |
| `v`  | 版本号，固定为 `2`                           |
| `c`  | 固定为 `1`                                   |

**示例**：搜索“茅台”

```text
https://smartbox.gtimg.cn/s3/?v=2&q=茅台&t=all&c=1
```

### 7.3 返回格式

返回纯文本，包含市场前缀、股票代码、股票名称、资产类型等信息。常见资产类型包括：`GP-A`（A股）、`ZS`（指数）、`fund`（基金/ETF）等。具体字段以实际返回为准。

---

## 八、指数与全球指数接口

### 8.1 指数实时行情

指数代码直接使用 `q=` 参数查询，与个股共用同一接口：

```text
http://qt.gtimg.cn/q=sh000001,sz399001,sz399006
```

| 指数         | 代码       |
| ------------ | ---------- |
| 上证指数     | `sh000001` |
| 深证成指     | `sz399001` |
| 创业板指     | `sz399006` |
| 恒生指数     | `hkHSI`    |
| 恒生科技指数 | `hkHSTECH` |

### 8.2 全球指数

```text
http://qt.gtimg.cn/q=GSPC,IXIC
```

| 指数             | 代码   |
| ---------------- | ------ |
| 标普500          | `GSPC` |
| 纳斯达克综合指数 | `IXIC` |

---

## 九、接口地址总览

| 接口名称     | 地址                                                            | 返回格式    |
| ------------ | --------------------------------------------------------------- | ----------- |
| 实时行情     | `http://qt.gtimg.cn/q=代码`                                     | 文本（GBK） |
| 盘口分析     | `http://qt.gtimg.cn/q=s_pk代码`                                 | 文本（GBK） |
| 简要信息     | `http://qt.gtimg.cn/q=s_代码`                                   | 文本（GBK） |
| 分时数据     | `https://web.ifzq.gtimg.cn/appstock/app/minute/query?code=代码` | JSON        |
| 日/周/月K线  | `https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=...`  | JSON        |
| 分钟K线      | `http://ifzq.gtimg.cn/appstock/app/kline/mkline?param=...`      | JSON        |
| 板块排名     | `https://proxy.finance.qq.com/cgi/cgi-bin/rank/pt/getRank?...`  | JSON        |
| 股票搜索     | `https://smartbox.gtimg.cn/s3/?v=2&q=关键词&t=all&c=1`          | 文本        |
| 指数实时行情 | `http://qt.gtimg.cn/q=sh000001,sz399001,sz399006`               | 文本（GBK） |
| 全球指数     | `http://qt.gtimg.cn/q=GSPC,IXIC`                                | 文本（GBK） |

---

## 十、使用注意事项

1. **编码问题**
   `qt.gtimg.cn` 系列接口返回 **GBK** 编码，必须使用 `gbk` 或 `gb2312` 解码，否则中文名称乱码。

2. **字段分隔符**
   基础行情接口使用波浪号 `~` 分隔字段，需按固定下标位置解析，空字段保留相邻的 `~`。

3. **请求频率**
   建议单次查询不超过 20 个代码，查询间隔保持在 **100ms 以上**，避免高频请求导致 IP 被封。

4. **延迟说明**
   A 股通常为实时数据，港股免费版有约 **15 分钟延迟**。

5. **User-Agent 与 Referer**
   部分情况下需要设置 `User-Agent` 和 `Referer: https://finance.qq.com` 请求头才能正常获取数据。

6. **非官方 API**
   以上接口是腾讯财经网页端使用的内部接口，未经正式文档化，腾讯可能随时调整或限制，建议做好异常处理和备用数据源方案。

7. **货币单位**
   港股返回港元/美元，美股返回美元，展示时必须标注正确的货币单位，禁止使用人民币符号。

8. **覆盖范围**
   腾讯公开接口主要覆盖 A 股、港股、美股、ETF、指数和部分期货、外汇。北交所部分标的可能不在覆盖范围内。
