# Api
PORT="3001"
API_PREFIX="/api"
ALLOWED_ORIGINS=

# JWT
JWT_ACCESS_EXPIRES_IN="15m"
JWT_ACCESS_SECRET_KEY="6YcVcnhk2i8LLzST/mwa5zQ3UEQ8nDLMfQrubGKm5Rk="
JWT_REFRESH_EXPIRES_IN="7d"
JWT_REFRESH_SECRET_KEY="qP4fFAD+xvvb1JLJ+ZGBgfmEyMjFSs7yUVszDBNkVTo="

# Auth - 多设备控制
AUTH_MAX_DEVICES="5"

# cocker-compose 使用
PG_USER="quant"
PG_PASS="quant.postgres"
PG_HOST="127.0.0.1"
PG_PORT="5432"
PG_DB="quant_db"

# 项目连接数据库时使用统一连接串 postgres://[user]:[pass]@host:port/db
PG_URL="postgres://${PG_USER}:${PG_PASS}@${PG_HOST}:${PG_PORT}/${PG_DB}"

# cocker-compose 使用
REDIS_USER="quant"
REDIS_PASS="quant.redis"
REDIS_HOST="127.0.0.1"
REDIS_PORT="6379"
REDIS_DB="0"

# 项目连接数据库时使用统一连接串 redis://[user]:[pass]@host:port[/db]
REDIS_URL="redis://${REDIS_USER}:${REDIS_PASS}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}"
REDIS_KEY_PREFIX="quant-"

# 通达信行情服务器(node-tdx-market)
# 留空则自动测速选择最快的服务器;指定可跳过测速、加快启动
# TDX_HOST="119.147.212.81"
# TDX_PORT="7709"
